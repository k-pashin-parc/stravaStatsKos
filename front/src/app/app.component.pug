div(
	*ngIf='activitiesDataService.getState("isInited")'
)
	app-header

	.content
		router-outlet

	.app-loader(*ngIf='activitiesDataService.getState("isLoading")')
		mat-progress-bar(
			mode='indeterminate'
		)
