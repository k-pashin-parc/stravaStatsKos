common-chart(
	[chartData]='chart.distance.data',
	[chartConfig]='chart.distance.config'
)

common-chart(
	[chartData]='chart.speed.data',
	[chartConfig]='chart.speed.config'
)

common-chart(
	[chartData]='chart.detailSpeed.data',
	[chartConfig]='chart.detailSpeed.config'
)

common-chart(
	[chartData]='chart.distanceByMonths.data',
	[chartConfig]='chart.distanceByMonths.config'
)
